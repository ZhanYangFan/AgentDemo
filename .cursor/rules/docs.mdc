---
description: 改策划案或验收时对齐文档优先级
globs: Docs/**/*.md
alwaysApply: false
---

# Docs

- `00` + `06` 管主循环与接口；与分册冲突时以它们为准。
- 分册 `01`～`05` 是模块规格，不是独立工程说明书。
- 改接口/Registry/存盘字段时，同步改 `06` 与 `AGENTS.md` 相关表。
- 开发流程写在 `Docs/开发规范/`，不要把流程塞进策划案正文。
